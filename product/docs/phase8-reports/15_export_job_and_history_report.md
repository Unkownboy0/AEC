# Phase 8 – Export Job and History Report

## Overview
This document specifies `ExportJob` tracking and user export download audit logs.

---

## Audit Log Fields
- `userId`, `userRole`, `action`, `documentType`, `targetEntityId`, `createdAt`.
- Endpoint: `GET /api/enterprise/exports/history`
