# Phase 8 – Database Migration Report

## Overview
This document records database schema updates and index additions for Phase 8.

---

## Added Models & Tables
1. `DigitalIdCard` (`digital_id_cards`)
2. `ProfileChangeRequest` (`profile_change_requests`)
3. `ExportJob` (`export_jobs`)
4. `DocumentDownloadAudit` (`document_download_audits`)
