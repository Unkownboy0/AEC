# Phase 9 – Report Scope and Permission Report

## Overview
This document specifies `ReportScopeResolver` scoping rules.

---

## Scoping Parameters
- HOD queries restricted by `departmentId`.
- Faculty queries restricted by `facultyId` and assigned classes.
- Student queries restricted by `studentId`.
