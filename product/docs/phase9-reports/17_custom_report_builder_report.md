# Phase 9 – Custom Report Builder Report

## Overview
This document specifies the custom report builder query engine and whitelisted dataset schemas.

---

## Whitelisted Datasets
`STUDENTS`, `FACULTY`, `ATTENDANCE`, `MARKS`, `LEAVES`, `TASKS`, `COMPLAINTS`, `PLACEMENTS`.
