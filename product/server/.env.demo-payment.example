# Isolated local/CI demo-payment overlay. Never use this file for production.
NODE_ENV=test
PAYMENT_GATEWAY=DEMO_PAYMENT
FIREBASE_SERVICE_ACCOUNT_JSON=
FIREBASE_SERVICE_ACCOUNT_PATH=

# Inherit DATABASE_URL and JWT_SECRET from the normal non-production environment,
# or copy this file outside the repository and add isolated test values there.
